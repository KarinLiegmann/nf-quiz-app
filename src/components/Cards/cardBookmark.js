import { createElement } from '../../lib/createElement'

export function createCardBookmark() {
    const cardBookmark = createElement('span', 'far', 'fa-bookmark', 'card__bookmark')
    return cardBookmark
}